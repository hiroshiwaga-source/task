# CLAUDE.md — task-boad

このファイルは Claude（および AI アシスタント）が本リポジトリで作業するときのガイドです。

## プロジェクト概要

**task-boad** はタスク管理（ボード形式を想定）向けのプロジェクトです。スタックやディレクトリ構成はリポジトリ内の `README.md` や設定ファイル（例: `package.json`、`pyproject.toml`）を正としてください。追加されたらここに追記してもよいです。

## GitHubリポジトリ

https://github.com/hiroshiwaga-source/task.git

## 実装・編集の原則

- 依頼された範囲に絞って変更する。無関係なリファクタやファイル追加はしない。
- 既存の命名、型、インポート、フォーマットに合わせる。
- 仕様が不明なときは推測で広げず、必要最小限の確認をする。

## Git / GitHub 運用（必須）

**コードを変更したタイミングで、その都度 Git にコミットし、リモート（GitHub）へプッシュする。**

1. 変更内容が論理的にまとまる単位でコミットする（1 コミットが巨大になりすぎないようにする）。
2. コミットメッセージは何を・なぜ変えたかが分かる日本語または英語で書く。
3. 作業の区切りごとに `git push` し、GitHub 上の既定ブランチ（通常 `main` または `master`）と同期を保つ。
4. プッシュ前に `git status` で意図しないファイルが含まれていないか確認する。
5. リモート未設定の場合は、ユーザーに GitHub リポジトリ URL の設定を依頼する（勝手にリモートは追加しない）。

### 推奨フロー（変更のたび）

```text
git add -A   （または必要なファイルのみ git add）
git commit -m "変更の要約"
git push origin <ブランチ名>
```

ブランチ戦略（feature ブランチのみプッシュする等）がプロジェクトで決まっている場合は、そのルールに従う。

## ドキュメント

- ユーザーが明示的に求めない限り、不要な README や長文ドキュメントを増やさない。
- プロジェクト固有のルールを追加するときは **この CLAUDE.md を更新**する。
